lib dependencies: concord, tesseract, curl

TODO:
refactor savetofile() to not need playerinfile and return a bool with weither
the player was or wasn't already present in the db

refactor parseline()
refactor on_help()
use / instead of prefix
detect wrong screenshot

raids
ascensions (track mats)
